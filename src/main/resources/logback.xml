<configuration>

    <!-- Konzol kimenet beállítása -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Napi info szintű naplózás -->
    <root level="info">
        <appender-ref ref="STDOUT"/>
    </root>

</configuration>